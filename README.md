# CV-Ex-5
This program detects moving vehicles in a video using background subtraction and identifies them via contours. It then applies optical flow to estimate motion within each detected region and calculates approximate speed in km/h. Bounding boxes and speed values are drawn on each frame for visualization.
